import React from 'react';
import { ListingContext } from '../context/ListingContext'


export default function Listing() {

    // const listingId = useContext(ListingContext);

    try{
        // const res = await axios.get(`http://localhost:5004/api/listing/${listing.listing_id}`)
    } catch(error) {
        
    }


}